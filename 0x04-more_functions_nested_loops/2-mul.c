#include "main.h"
/**
 * mul - entry point
 *
 * Description: functions and nested loops
 *
 * @a: first parameter expects an integer
 * @b: second parameter expects an integer
 *
 * Return: returns a product of two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
